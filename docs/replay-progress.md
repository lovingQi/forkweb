# 日志诊断与回放工具开发现状

本文记录 forkweb 日志诊断与回放工具的设计结论、当前实现状态、测试结果、使用方式、已知问题和后续计划。

## 研究结论

- 现有 forkweb 通过 `/ws/high` 接收 `pose`、`vel`、`laser_data`、`path_points`、`clearances`、`robot_size`。
- 现有 forkweb 通过 `/ws/low` 接收 `status`、`battery`、`score`、`alarm`、`current_routes`、`fork_info`、IO 等状态。
- 日志中 `FltStatus: RmstoSendArg` 可提供离线回放主数据源，包括位置、朝向、电量、定位分、任务 ID、错误、货叉高度等。
- 日志中 `InfoStatus: ROBOT STATUS` 可补充状态、速度、电机、充电、定位分等。
- 日志中 `error_name,error_str=ERRORxxxx,{...}` 可建立错误码字典，正文中的 `ERRORxxxx` 可建立错误发生点。

## 已选方案

- 直接集成到 forkweb。
- 新增 `/replay` 页面，不改动现有实时监控首页。
- 使用 Node.js + TypeScript 本地辅助服务处理日志目录、地图文件、解析、回放和报告。
- 第一版本地优先，后续预留车端部署。
- 地图选择支持地图目录自动加载，失败时可手动填写地图文件。
- 报告导出支持 Markdown 和 JSON。

## 当前架构

```text
forkweb
├── replay-server/              # 本地日志诊断服务
│   ├── src/index.ts            # HTTP + WebSocket 入口
│   ├── src/parser/             # 日志、状态、错误码、任务解析
│   └── src/core/               # session、timeline、噪声折叠、根因、缓存、诊断包、报告生成
├── src/views/Replay.vue        # 日志诊断页面
├── src/api/replay.ts           # 诊断 API 封装
├── src/stores/replay.ts        # 诊断状态
└── src/stores/robot.ts         # 支持实时/诊断 WebSocket 连接
```

### 后端职责

- 读取日志目录下的 `.log` 文件。
- 按时间排序并解析日志行。
- 从 `FltStatus: RmstoSendArg` 和 `InfoStatus: ROBOT STATUS` 生成回放帧。
- 解析 `ERRORxxxx` 错误码定义和发生点。
- 构建问题时间线、任务段、噪声折叠结果。
- 从 jarvis-fork 源码补充扫描 `ERRORxxxx` 错误码字典。
- 输出第一版根因候选，并支持根因反馈记录。
- 支持地图别名关系保存，用于低可信地图匹配后的人工确认。
- 支持诊断包导出和导入，包含日志、地图和 manifest。
- 支持缓存统计和清理。
- 模拟 forkweb 已有车端接口：`/api/state`、`/api/map`、`/ws/high`、`/ws/low`。
- 输出 Markdown 和 JSON 报告。

### 前端职责

- 提供 `/replay` 日志诊断页面。
- 配置日志目录、地图目录、手动地图文件。
- 展示概览、地图回放、Top 问题、时间线、错误码中心、任务视角、原始日志过滤。
- 复用 `CanvasView` 显示地图和车辆位置。
- 提供播放、暂停、倍速、进度条拖动跳转。
- 提供诊断包导入/导出、缓存查看/清理、根因反馈和日志上下文复制。
- 提供地图别名管理、时间线高级筛选、错误码筛选、折叠日志分页和缓存分类管理。

## 第一版范围

- 一键概览页。
- 问题时间线。
- 地图回放与事件跳转。
- 错误码中心。
- 噪声折叠与智能过滤。
- 任务视角。
- 原始日志/过滤 Tab。
- Top 问题默认展示 5 条，可由后续交互扩展。

## 已完成

- 新增 replay-server 本地诊断服务骨架。
- 新增日志解析模块。
- 新增 FltStatus 和 InfoStatus 回放帧解析。
- 新增 ERRORxxxx 错误码定义与发生解析。
- 新增任务段解析。
- 新增 timeline、噪声折叠、报告生成。
- 新增兼容 `/api/state`、`/api/map`、`/ws/high`、`/ws/low`。
- 新增前端 replay API、store、路由和页面。
- 新增日志诊断菜单入口。
- 新增类似视频播放器的回放进度条。
- 支持显示当前回放时间和总时长。
- 支持拖动进度条调用 `seek`。
- 支持播放时进度条自动跟随后端播放状态推进。
- 增强概览页，补充车辆、地图、分支和数据完整性标签。
- 增强时间线，支持事件分类、类型过滤和前后原始日志上下文。
- 增强地图回放，支持轨迹线和错误/任务事件点。
- 增强错误码中心，支持错误码聚合表和发生点联动。
- 增强原始日志过滤，支持错误码、任务 ID、噪声、关键事件和分页。
- 增强任务视角，补充完成任务、成功状态、未完成路径等字段。
- 增强地图匹配，支持从日志识别地图名、匹配策略、置信度和低可信提醒。
- 增强错误码分类，区分真实故障、配置提醒、定义和未知文本。
- 新增第一版根因候选分析，输出疑似根因、置信度、证据和处理建议。
- 新增真实时间/按状态帧两种回放模式。
- 新增进度条事件刻度，支持点击跳转。
- 新增轨迹点/事件点点击选择并跳转到对应状态。
- 新增轻量 session 缓存和“重新解析”入口。
- 增强 Markdown/JSON 报告，补充诊断结论、地图匹配、根因候选和错误码分组。
- 新增地图别名配置和确认入口，低可信匹配时可保存“日志地图名 -> 本地地图文件”的关系。
- 新增从 jarvis-fork 源码扫描 `ERRORxxxx` 的错误码字典补充能力。
- 新增根因规则拆分、证据加权排序和“有用/误报”反馈入口。
- 增强任务解析，补充失败候选、轨迹帧范围、失败前后日志上下文等字段。
- 新增状态曲线组件，展示定位分、电量、速度、货叉高度、错误数量，并支持点击曲线跳转时间。
- 增强原始日志过滤，支持开始/结束时间戳、当前时间上下文、复制上下文、折叠日志详情。
- 新增诊断包导出/导入：
  - 导出 zip，包含当前日志、匹配地图和 manifest。
  - 导入 zip 后可在无实车连接时直接加载离线日志和地图。
- 新增缓存管理接口和前端入口，可查看缓存文件数量/大小并清理缓存。
- 新增地图别名管理弹窗，支持列表、删除、导入、导出和冲突提示。
- 增强时间线筛选，支持时间范围、严重度、真实故障/配置提醒/噪声快捷切换、严重度排序和重复事件合并。
- 增强时间线与原始日志联动，点击事件后自动刷新对应日志上下文。
- 增强错误码中心筛选，支持错误码、模块、等级、类型筛选，并可跳转查看原始日志上下文。
- 增强折叠日志详情，支持分页查看完整折叠日志和复制当前页。
- 增强 Markdown/JSON 报告，补充文件索引、关键时间线摘要、根因证据、任务摘要和结构化 JSON 字段。
- 增强诊断包导出，zip 内包含 `report.md`、`report.json`、`map-alias.json`、根因反馈摘要。
- 增强诊断包导入，展示包信息并返回包内别名及冲突提示，不静默覆盖本地别名。
- 增强缓存管理，支持分类统计、按类型清理和自动清理过期缓存。
- 新增 `npm run replay:dev:all` 一键启动脚本，可自动选择端口、写入 `public/config.js` 并同时启动后端和前端。
- 增强时间线 Web/服务启动识别，新增服务启动类事件分类和前端中文标签。
- 增强错误码与任务关联：没有显式任务 ID 的错误码发生点会按发生时间匹配所在任务段。
- 增强任务视角：补充 `current_routes` 路线摘要、失败上下文数量、失败日志入口。
- 增强时间线/错误码/任务联动：点击事件或发生点后自动切换到原始日志 Tab，并高亮关联日志行。
- 增强进度条事件 marker：保留最多 200 个 marker 的保护逻辑，同时显示被隐藏的聚合 marker 数量。
- 增强诊断包导入：新增本地 zip 路径导入接口和前端入口，适合大诊断包离线加载。

## 当前使用方式

### 启动诊断服务

```bash
cd /home/xbl/Desktop/learn/forkweb
npm run replay:dev
```

默认地址：

```text
http://127.0.0.1:18080
```

本次验证时 `18080` 已被其他进程占用，因此使用：

```bash
cd /home/xbl/Desktop/learn/forkweb
REPLAY_PORT=18081 npx -y -p node@20 -c 'npm run replay:dev'
```

本次验证服务地址：

```text
http://127.0.0.1:18081
```

本轮新增一键启动脚本后，实际验证自动选择：

```text
后端: http://127.0.0.1:18082
前端: http://127.0.0.1:5174/replay
```

### 启动前端

当前系统 Node 是 v16.20.2，Vite 5 需要 Node 18+。本机可临时使用 Node 20 启动：

```bash
cd /home/xbl/Desktop/learn/forkweb
npx -y -p node@20 -c 'npm run dev -- --host 127.0.0.1'
```

默认访问：

```text
http://127.0.0.1:5173/replay
```

推荐使用一键启动：

```bash
cd /home/xbl/Desktop/learn/forkweb
npx -y -p node@20 -c 'npm run replay:dev:all'
```

### 默认诊断路径

默认值在 `src/stores/replay.ts`：

```text
日志目录: /home/xbl/Desktop
地图目录: /home/xbl/Desktop/jarvis-fork/params/map
地图文件: 空，自动匹配
```

当前默认会加载 `/home/xbl/Desktop` 下的 `.log` 文件，例如：

```text
/home/xbl/Desktop/log-20260710-173240.log
```

如果要诊断其他日志，在页面顶部修改“日志目录”。如果自动地图不符合预期，在“地图文件”里填写具体地图 JSON 的完整路径。

## 当前页面能力

- 概览卡片：
  - 时间范围
  - 日志文件数
  - 回放帧数
  - 任务数
  - 错误事件数
  - 错误码数
  - 车辆、地图、分支
  - 地图/回放帧/任务 ID/错误码定义完整性标签
  - 地图匹配策略和置信度
  - 数据提醒数量和具体提醒
- 地图回放：
  - 播放
  - 暂停
  - 倍速
  - 进度条拖动跳转
  - 当前时间/总时长
  - 轨迹线
  - 事件点
  - 当前状态详情
  - 真实时间/按状态帧模式切换
  - 进度条事件刻度
  - 点击轨迹点或事件点跳转
  - 轨迹平滑开关
  - 点击轨迹点查看详情浮层
- Top 问题：
  - 默认展示 5 条
  - 点击可跳转到对应时间
- 时间线：
  - 展示 system/connect/config/warning/error/status/task/error_code 等事件
  - 支持低定位分、lost、estop 等状态类事件
  - 支持按事件类型过滤
  - 支持展开前后原始日志片段
  - 点击行可跳转地图时间
  - 支持时间范围、严重度、故障类型、排序和重复事件合并
  - 点击事件可联动刷新原始日志上下文
  - 支持 Web/服务启动类事件分类展示
  - 点击事件后可自动切换到原始日志 Tab 并高亮关联日志行
- 错误码中心：
  - 按错误码聚合展示次数、等级、首次/末次时间、描述
  - 统计真实故障次数和配置提醒次数
  - 点击错误码后展示发生点
  - 关联错误码说明
  - 展示错误码来源、源码/日志字典置信度
  - 点击发生点跳转地图时间
  - 支持错误码、模块、等级、类型筛选
  - 支持发生点查看原始日志上下文
  - 支持按时间匹配任务段补充关联任务
  - 查看发生点日志上下文时可自动切换并高亮原始日志
- 任务视角：
  - 基于 `current_task_id` 切分任务段
  - 展示开始/结束时间、状态、完成任务、成功状态、未完成路径、关联错误
  - 点击任务后联动时间线、错误码、轨迹和原始日志过滤
  - 展示 `current_routes` 路线摘要
  - 支持点击任务失败上下文跳转到原始日志
- 原始日志/过滤：
  - 按级别、模块、关键词、错误码、任务 ID、时间范围、噪声、关键事件过滤
  - 支持当前时间上下文
  - 支持复制当前页/上下文日志
  - 支持分页
  - 显示噪声折叠统计
  - 点击折叠噪声标签可分页查看完整折叠日志
  - 支持复制折叠详情当前页
- 报告导出：
  - Markdown/JSON 包含诊断结论、地图匹配、根因候选、错误码分组、任务证据、文件索引和关键时间线摘要。
- 诊断包：
  - 支持导出当前诊断 zip。
  - 支持导入诊断 zip 并自动加载离线日志/地图。
  - zip 内包含报告、分析 JSON、地图别名配置和根因反馈摘要。
  - 支持本地 zip 路径导入，避免大包全部经浏览器 base64 传输。
- 缓存管理：
  - 支持查看缓存文件数、大小和分类明细。
  - 支持一键清理缓存和按类型清理缓存。

## 第一版功能对照

本节对照前期讨论过的 A/B/C/D/E/G 功能点，标记当前实现状态和后续需要补齐的内容。

### A. 一键概览页

当前已完成：

- 日志时间范围。
- 日志文件数量。
- 车辆名。
- 版本/分支/提交信息。
- 地图名或手选地图名。
- 状态采样数量，以“回放帧”展示。
- 任务数量。
- 错误码数量。
- `[E]` 和 `[W]` 数量。
- 错误事件数量。
- Top 问题列表。
- 点击 Top 问题后跳转到对应回放时刻。
- 增加数据完整性提示：
  - 是否加载到地图。
  - 是否存在可回放帧。
  - 是否存在任务 ID。
  - 是否存在错误码定义。
- 地图匹配策略、置信度和低可信数据提醒。
- 第一版根因候选结论。
- 根因候选支持展开证据、跳转证据、标记有用/误报。

后续优化：

- 增加日志包来源、加载耗时、解析耗时等运行信息。
- 将根因候选进一步压缩成现场人员能直接看的 3 条以内结论。

### B. 问题时间线

当前已完成：

- 按时间排序。
- `[E]` 级日志事件。
- 关键 `[W]` 日志事件。
- 错误码发生事件。
- 错误码配置提醒和真实故障分级。
- 状态变化事件。
- 任务变化事件。
- 系统启动类事件。
- 连接成功类事件。
- 地图/参数加载异常分类。
- 低定位分事件。
- lost/estop 等状态异常分类。
- 按事件类型过滤。
- 展示事件前后原始日志片段。
- 点击时间线事件后跳转到地图回放时刻。
- 支持时间范围过滤。
- 支持只看“真实故障/配置提醒/噪声”的快捷切换。
- 支持相邻重复事件合并。
- 支持按严重度排序视图。
- 点击时间线事件后自动刷新原始日志上下文。

后续优化：

- 根据更多现场日志继续增加 Web/服务启动事件模式。
- 继续优化重复事件合并规则，避免误合并不同根因的相似日志。

### C. 地图回放 + 事件跳转

当前已完成：

- 基于 `FltStatus: RmstoSendArg` 的 `x/y/theta` 生成回放帧。
- 基于 `InfoStatus: ROBOT STATUS` 的 `loc(pose...)` 辅助补充。
- 复用 `CanvasView` 显示地图和当前车辆位置。
- 播放。
- 暂停。
- 倍速。
- 视频式进度条。
- 拖动时间轴跳转。
- 点击事件跳转。
- 轨迹线。
- 事件点地图标记。
- 当前时刻状态/任务/错误详情面板。
- 真实时间/按状态帧回放模式。
- 进度条错误/任务事件刻度。
- 点击轨迹点查看当时状态。
- 轨迹平滑回放开关。
- 轨迹点详情浮层。

后续优化：

- 回放时支持“跟随车辆”和“自由查看地图”两种模式。

### D. 错误码中心

当前已完成：

- 解析 `error_name,error_str=ERRORxxxx,{...}` 错误码定义。
- 解析日志正文中的 `ERRORxxxx`。
- 解析 `RmstoSendArg.errors` 中出现的错误码。
- 错误发生点列表。
- 错误码说明展示。
- 按错误码聚合展示：
  - 错误码。
  - 描述。
  - 屏幕提示。
  - 等级。
  - 发生次数。
  - 首次/末次时间。
- 点击错误码查看所有发生点。
- 点击错误发生点跳转到地图时刻。
- 区分错误码定义、错误码配置提醒、真实故障发生。
- 聚合统计真实故障次数和配置提醒次数。
- 从 jarvis-fork 源码扫描 `ERRORxxxx` 补充字典。
- 展示错误码来源和字典置信度。
- 支持按错误等级、模块、错误码、发生类型筛选。
- 错误码发生点支持查看原始日志上下文。

部分完成：

- `current_code is ERRORxxxx` 和 `current_task_error_code is ERRORxxxx` 的模式已纳入通用 `ERRORxxxx` 捕获，但样例日志中多数为空，需要更多真实故障日志验证。

后续优化：

- 用更多真实任务日志继续校准错误码按时间匹配任务段的准确性。
- 源码错误码字典目前为扫描式补充，后续可按真实错误码定义文件做结构化解析。

### E. 噪声折叠与智能过滤

当前已完成：

- 内置噪声折叠：
  - 高频 `sent: toSendV`。
  - 高频 `FltTask: start calc current task id in FltTask`。
  - 高频 `unfinshed path ... null`。
  - 高频 `Invalid task error code:` 空值。
  - 重复 `Get charging error state is: 0`。
  - 重复 `get battery failed`。
- 原始日志过滤：
  - 等级 `[E]/[W]/[I]/[D]`。
  - 模块名。
  - 关键词。
  - 错误码过滤。
  - 任务 ID 过滤。
  - 是否噪声过滤。
  - 是否关键事件过滤。
- 配置提醒分类。
- 原始日志分页。
- 时间范围过滤。
- 当前时间上下文过滤。
- 复制原始日志片段。
- 重复日志簇详情，点开后可分页查看完整折叠日志。
- 支持复制折叠日志当前页。

后续优化：

- 保存常用过滤条件，例如“只看真实错误”“只看任务失败前后”。
- 噪声规则支持配置化，方便后续根据现场反馈追加规则。
- 折叠详情后续可增加按时间范围二次过滤。

### G. 任务视角

当前已完成：

- 基于 `current_task_id` 切分任务段。
- 展示任务列表 Tab。
- 每个任务段包含开始/结束时间、状态、错误、帧数。
- 解析 `last_finished_task_id`。
- 解析 `last_finished_task_is_success`。
- 解析 `unfinished_path`。
- 解析 `new_unfinished_path`。
- 展示任务相关错误。
- 解析任务失败候选线索。
- 记录任务轨迹帧范围。
- 支持任务点击后联动过滤时间线、错误码、轨迹和原始日志。
- 支持任务失败前后日志上下文字段。
- 支持 `current_routes` 路线摘要展示。
- 支持点击任务失败上下文跳转原始日志。

部分完成：

- 当前样例日志中 `current_task_id` 始终为 `Null`，因此任务视角为空。
- `status` 已用于任务段状态，但仍需要更多真实任务日志验证。

后续优化：

- 解析更多 `FltTask` 相关任务日志。
- 使用更多真实日志完善 `current_routes` 结构化字段。
- 展示任务期间轨迹的更明确范围和地图高亮。
- 展示任务失败前后日志的完整弹窗视图。
- 使用真实任务日志继续校验联动效果。

## 验证记录

- 本轮优化后验证通过：
  - `npx -y -p node@20 -c 'npm run typecheck'` 通过。
  - `npx -y -p node@20 -c 'npm run build'` 通过，仍存在 Vite/Rollup chunk 体积提示。
  - `npx -y -p node@20 -c 'npm run replay:build'` 通过。
  - `npx -y -p node@20 -c './node_modules/.bin/tsx replay-server/scripts/verify-sample.ts'` 通过。
  - 样例验证仍生成回放帧 601、根因候选 3 条、错误码发生点 41 个，诊断包导出/导入和缓存摘要正常。
  - 当前一键启动服务保持运行，`public/config.js` 指向 `http://127.0.0.1:18082/api`，前端进程包含 `http://127.0.0.1:5174/replay`。
- `npx -y -p node@20 -c 'npm run typecheck'` 通过。
- `npx -y -p node@20 -c 'npm run build'` 通过。
- `npx -y -p node@20 -c 'npm run replay:build'` 通过。
- `npx -y -p node@20 -c './node_modules/.bin/tsx replay-server/scripts/verify-sample.ts'` 通过。
- `npm run replay:build` 通过。
- `npm run typecheck` 通过。
- 使用 `/home/xbl/Desktop/log-20260710-173240.log` 验证：
  - 识别日志文件 1 个。
  - 解析日志行 165902 条。
  - 生成回放帧 601 个。
  - 错误码字典 80 个。
  - 生成错误事件 9 个，错误码发生点 41 个。
  - `[E]` 日志 9 条。
  - `[W]` 日志 434 条。
  - 当前样例日志 `current_task_id` 始终为 `Null`，因此任务段为 0。
- API 抽样验证通过：
  - `/api/replay/overview` 可返回完整性、统计和 Top 问题。
  - `/api/replay/overview` 可返回 `mapMatch`、`dataWarnings` 和 `rootCauses`。
  - `/api/replay/frames` 返回 601 个回放帧。
  - `/api/replay/error-codes` 返回错误码定义、发生点和聚合项。
  - `/api/replay/error-codes` 可区分真实故障次数和配置提醒次数。
  - `/api/replay/events?mode=config_notice&sort=severity&dedupe=true` 可返回配置提醒筛选结果。
  - `/api/replay/error-codes?kind=config_notice&code=ERROR05` 可返回错误码筛选结果。
  - `/api/replay/logs?aroundTimeMs=1783675961538&aroundLines=3&limit=20` 返回 7 行上下文、复制文本和 9 类折叠统计。
  - `/api/replay/folded-logs/sent_speed/lines?limit=5` 可返回折叠日志分页和复制文本。
  - `/api/replay/seek` 支持 `timeMs` 和 `frameIndex`。
  - `/api/replay/cache` 返回缓存目录、文件数、大小和分类 buckets。
  - `/api/replay/package` 可下载诊断包 zip，包内包含报告、分析 JSON、地图别名和根因反馈摘要。
  - `/api/state`、`/api/map`、`/api/replay/report.md`、`/api/replay/report.json` 均可返回数据。
- `replay-server/scripts/verify-sample.ts` 验证通过：
  - 回放帧 601。
  - 地图检测到日志中的 `3.json`。
  - 本地地图目录未找到 `3.json`，因此低可信回退到 `JA.json` 并给出数据提醒。
  - 根因候选 3 条。
  - 诊断包可导出、导入，并能重新解析出回放帧。
  - 缓存摘要可返回文件数、大小和分类 buckets。
  - 报告包含诊断结论、地图匹配、根因候选、文件索引、关键时间线字段。
- 无头 Chrome 测试 `/replay` 页面通过：
  - 页面可渲染“日志诊断”。
  - 点击“加载诊断”后可显示 601 个回放帧、9 个错误事件、80 个错误码。
  - 页面可显示“地图已加载”“回放帧可用”“错误码定义可用”等完整性标签。
  - 页面可显示地图匹配策略、数据提醒和低可信地图风险。
  - 页面存在地图 canvas。
  - 页面存在进度条。
  - 页面存在 9 个进度条事件 marker。
  - 页面存在“真实时间/按状态帧”回放模式切换。
  - 页面存在时间线、错误码中心、任务视角、原始日志/过滤 Tab。
  - 原始日志/过滤 Tab 存在“当前时间上下文”“复制上下文”和折叠噪声标签。
  - 诊断包导出/导入入口、缓存查看/清理入口可渲染。
- 本轮无头 Chrome 测试 `/replay` 页面通过：
  - 页面可渲染地图别名管理入口和弹窗。
  - 页面可渲染时间线高级筛选入口。
  - 页面可渲染错误码发生点“查看日志上下文”入口。
  - 页面可渲染折叠日志详情分页和“复制当前页”入口。
  - 页面可渲染缓存详情弹窗和分类缓存列表。

## 当前环境注意

- 当前本机 Node 为 v16.20.2。
- Vite 5 和部分新包声明需要 Node 18+，因此 `npm run dev` 在当前 Node 下无法启动，错误为 `crypto.getRandomValues is not a function`。
- 已选用兼容 Node 16 的 `express@4` 和 `tsx@3.14.0`，诊断服务构建可通过。
- 本次验证前 `127.0.0.1:18080` 和 `127.0.0.1:18081` 已被其他进程占用；一键启动脚本自动选择新版后端 `18082` 和前端 `5174`。
- `public/config.js` 默认指向 `18080`，一键启动脚本会在运行时自动写入实际 replay 端口。

## 已知问题

### P0：会影响诊断结论可信度

- 当前样例日志检测到地图名 `3.json`，但本地地图目录只回退加载到 `JA.json`。工具已经提示低可信地图匹配，后续需要拿到现场实际 `3.json` 或建立地图别名关系，否则回放位置可能和现场地图不一致。
- 根因候选分析还是规则第一版，只能输出初步“疑似根因 + 证据链 + 置信度”，还需要更多真实故障日志校准规则和置信度。
- 错误码字典主要来自日志内 `error_name,error_str=ERRORxxxx`，后续仍需要从 jarvis-fork 源码或配置中补齐完整错误码字典。

### P1：会影响第一版现场使用体验

- 任务视角仍缺少真实任务日志验证：当前样例日志 `current_task_id` 始终为 `Null`，任务列表为空是合理结果，但任务切分、任务失败前后日志、任务期间轨迹还需要用真实任务日志验证。
- 进度条事件刻度当前只展示最多 200 个关键事件，后续需要在大日志里做聚合/缩放，避免 marker 太密。
- 诊断包导入已新增本地 zip 路径导入；浏览器选择文件仍使用 base64，后续如需远程网页上传大包，可继续补分片上传。
- 端口和运行时配置已有一键启动脚本处理，后续仍可进一步做成桌面应用或系统服务。

### P2：会影响大日志和长期使用

- 会话缓存已有分类统计、手动清理、按类型清理和过期自动清理；后续还没有缓存版本迁移策略。
- 诊断样本还不够丰富：当前主要用 `/home/xbl/Desktop/log-20260710-173240.log` 验证，后续需要覆盖有真实任务 ID、真实任务失败、定位异常、急停、设备离线、地图加载失败等典型日志。
- 报告已加入诊断结论、地图匹配、根因候选、错误码分组、文件索引、关键时间线和任务摘要；后续可继续补充更完整的人工处置建议。

## 后续优化点

- 地图匹配：
  - 已支持地图别名关系，例如日志中的 `3.json` 对应本地 `JA.json`。
  - 已支持别名管理列表、冲突提示和别名导入/导出。
  - 后续优化别名冲突的一键合并/覆盖确认流程。
- 错误事件降噪：
  - 启动阶段配置缺失类 warning 单独归类为“配置提醒”。
  - 根据更多真实日志继续补充噪声规则。
- 根因候选分析：
  - 使用更多真实故障日志校准置信度。
  - 已增加根因候选“有用/误报”反馈入口。
  - 后续补充反馈统计展示和规则权重回放。
- 任务视角增强：
  - 使用更多日志模式识别任务开始、任务结束、任务失败。
  - 支持按任务筛选时间线、错误码和轨迹。
- 状态曲线：
  - 已支持定位分、电量、速度、货叉高度、错误数量曲线。
  - 后续支持局部缩放、框选时间范围、曲线与日志上下文联动。
- 原始日志体验：
  - 已支持当前时间前后 N 行上下文。
  - 已支持复制原始日志片段。
  - 已支持按时间范围过滤。
  - 已支持点击时间线事件刷新原始日志上下文。
  - 后续支持点击事件自动切换到原始日志 Tab 并高亮对应行。
- 报告增强：
  - 已增加关键上下文片段。
  - 已增加任务摘要。
  - 已增加地图/日志文件索引。
  - 后续补充更明确的人工处置建议和处置状态。
- 性能优化：
  - 已支持手动缓存统计、分类清理和自动清理。
  - 后续补充缓存版本迁移。
  - 增量解析和索引缓存。
- 诊断包：
  - 已支持 zip 导入。
  - 已支持导出包含日志、地图、manifest、报告、JSON 分析结果、别名配置、根因反馈摘要的诊断包。
  - 已支持本地 zip 路径导入。
  - 后续改进远程大包上传方式和别名冲突确认交互。
- 部署：
  - 本地诊断稳定后，评估车端部署模式。
  - 已新增一键启动脚本。
  - 后续评估打包成桌面应用。
- 回放体验：
  - 已支持轨迹平滑回放开关。
  - 已支持事件点聚合显示。
  - 已支持轨迹点详情浮层。
  - 后续支持跟随车辆/自由查看模式和区间循环播放。

## 后续优化发散池

本节记录暂不立即开发、但后续值得持续评估的优化方向。这里的内容是发散池，不代表马上进入实现；每次进入开发前仍需要从中挑选一小批，重新形成明确计划和验证标准。

### P0：诊断可信度和真实样本

- 建立真实样本库：
  - 覆盖真实任务 ID 日志。
  - 覆盖真实任务失败日志。
  - 覆盖定位异常日志。
  - 覆盖急停日志。
  - 覆盖设备离线日志。
  - 覆盖地图加载失败日志。
  - 覆盖错误码真实发生日志。
  - 每类样本都记录期望识别结果，用于后续回归测试。
- 根因规则可解释化：
  - 展示触发了哪些根因规则。
  - 展示加分证据，例如错误码、状态变化、任务失败、低定位分。
  - 展示扣分证据，例如只有配置提醒、缺少真实错误发生点。
  - 展示置信度来源，避免现场人员只看到一个不透明分数。
  - 将“疑似根因”压缩为现场人员优先处理的 3 条以内结论。
- 错误码字典结构化：
  - 优先读取车端日志中的错误码定义。
  - 其次读取 jarvis-fork 真实配置或定义文件。
  - 再使用源码扫描补充错误码说明。
  - 最后才使用日志正文和文本上下文猜测。
  - 在错误码中心展示字典来源优先级和可信度。
- 任务视角真实校准：
  - 用真实任务日志校验任务开始、结束、失败识别。
  - 校验 `current_routes` 的结构化解析。
  - 校验任务期间轨迹范围是否准确。
  - 校验任务失败前后日志是否能命中真正失败点。
  - 校验错误码按时间匹配任务段是否会误关联。

### P1：现场排查效率

- 诊断首页进一步聚合：
  - 展示本次最可能根因 Top 3。
  - 展示建议优先查看的时间点。
  - 展示数据完整性评分。
  - 展示日志质量评分，例如是否缺地图、缺回放帧、缺任务 ID、缺错误码定义。
  - 展示加载耗时、解析耗时、日志包来源等运行信息。
- 时间线书签和备注：
  - 支持把某个事件标记为关键事件。
  - 支持给事件添加人工备注。
  - 支持在报告导出时带上人工标记和备注。
  - 支持在诊断包中保存书签，方便研发接手继续看。
- 原始日志增强：
  - 支持多关键词搜索。
  - 支持关键词高亮。
  - 支持按前后 N 秒查询上下文，而不只是前后 N 行。
  - 支持同一时间附近多模块日志分组展示。
  - 支持快速复制“当前事件证据包”，包含事件、地图时刻、前后日志。
- 回放体验增强：
  - 支持上一事件、下一事件。
  - 支持区间循环播放。
  - 支持回放到异常点自动暂停。
  - 支持回放截图，用于报告和问题单。
  - 支持跟随车辆和自由查看地图两种视角模式。

### P2：大日志性能和长期维护

- 增量解析和索引缓存：
  - 按文件 mtime、size、hash 建立日志索引。
  - 只重解析变化的日志文件。
  - 原始日志过滤尽量走索引，减少每次全量扫描。
  - 缓存中记录解析版本，避免字段结构变化后读到旧数据。
- 服务端分页：
  - 时间线支持服务端分页。
  - 错误码发生点支持服务端分页。
  - 原始日志支持按时间窗口查询。
  - 折叠日志详情支持时间范围二次过滤。
- marker 多级聚合：
  - 按小时、分钟、秒聚合事件 marker。
  - 点击聚合点展开事件列表。
  - 随时间轴缩放改变 marker 展示密度。
  - 对错误、警告、任务变化分别显示聚合数量。
- 解析进度显示：
  - 展示读取日志文件进度。
  - 展示解析状态帧进度。
  - 展示构建错误码和时间线进度。
  - 展示加载地图进度。
  - 大日志解析时前端显示明确的阶段状态，避免误以为页面卡死。
- 缓存版本迁移：
  - 缓存结构变更时自动失效。
  - 记录缓存版本升级历史。
  - 缓存清理策略支持配置化，例如最大保留天数、最大容量。
  - 区分会话缓存、诊断包缓存、反馈缓存的保留策略。

### P3：协作、部署和产品化

- 诊断包瘦身：
  - 支持按时间范围导出诊断包。
  - 支持只导出关键事件前后 N 分钟。
  - 支持选择是否包含地图、报告、别名、根因反馈。
  - 支持导出前预估诊断包大小。
- 诊断包人工结论：
  - 保存人工备注。
  - 保存已确认根因。
  - 保存处理状态，例如待确认、已复现、已定位、已修复。
  - 保存现场人员、车辆、地点、测试轮次等信息。
  - 报告中单独输出人工结论区。
- 诊断包对比：
  - 对比两个诊断包的版本、分支、提交信息。
  - 对比地图匹配结果。
  - 对比错误码出现次数和首次发生时间。
  - 对比任务失败点。
  - 对比定位分、电量、速度等状态曲线差异。
- 桌面应用：
  - 封装 Node、前端和 replay-server。
  - 打开即用，减少现场人员处理命令和端口。
  - 支持选择日志目录、地图目录、诊断包路径。
  - 支持应用内检查版本和导出环境信息。
- 车端轻量部署：
  - 只读访问车端日志和地图目录。
  - 支持一键打包诊断包下载到本机。
  - 限制 CPU、IO 和并发，避免影响车端运行。
  - 明确网络访问和权限边界。
- 一键采集脚本：
  - 输入时间范围后自动收集相关日志。
  - 自动收集地图、版本信息、配置文件、错误码字典。
  - 自动生成 manifest，减少人工漏传文件。
  - 支持现场人员一键生成可导入的诊断包。

### 候选功能边界说明

- 本节内容只是后续优化发散池，不代表马上开发。
- 后续进入执行前，需要从中挑选一小批形成具体实现计划。
- 优先级建议：
  - 先做真实样本库、错误码字典结构化和根因可信度校准。
  - 再做现场排查效率，例如书签、备注、上一事件/下一事件。
  - 然后做大日志性能，例如增量解析、服务端分页和 marker 多级聚合。
  - 最后考虑桌面应用、车端部署和一键采集脚本。
- 每个候选功能都需要配套验证方式，避免只增加界面入口而没有可验证的诊断收益。

## 2026-07-11 后续优化发散池首轮全量落地记录

本轮目标是尽可能一次性实现“后续优化发散池”中已经明确有价值的第一批能力，重点覆盖现场排查效率、大日志性能基础、协作交接和产品化准备。

### 本轮新增能力

- 样本库与回归验证：
  - 新增 `replay-server/samples/manifest.example.json`，用于记录样本路径和期望解析结果。
  - 新增 `npm run replay:verify:samples`，可按 manifest 批量验证回放帧、错误码、任务和根因关键词。
  - 增强 `replay-server/scripts/verify-sample.ts`，校验根因解释字段、健康评分、日志质量评分、推荐关注时间、解析统计、错误码来源、书签和人工结论导出。
- 根因可解释化：
  - `RootCauseCandidate` 增加触发规则、正向证据、反向证据、置信因素。
  - Markdown/JSON 报告输出根因解释信息。
  - 前端诊断结论区域展示规则、证据和置信因素，并支持复制根因证据包。
- 错误码字典结构化：
  - 错误码定义增加 `sourceKind`、`sourceLabel`、`sourcePriority`、`confidenceReason`、`dictionaryConfidence`。
  - 新增错误码来源优先级模块，区分日志定义、源码配置、源码扫描和文本猜测。
  - 错误码中心展示来源标签、来源说明和置信度。
- 诊断首页增强：
  - 概览显示健康分、日志质量分、解析耗时、缓存命中状态。
  - 新增建议优先查看时间点，点击后可跳转回放时刻。
- 时间线和书签：
  - 新增书签 API、存储和前端入口。
  - 支持从根因、时间线事件、错误码发生点或当前回放时刻添加人工书签。
  - 支持书签删除、跳转，并在报告和诊断包中保留。
- 原始日志增强：
  - 支持多关键词搜索，使用逗号分隔。
  - 支持关键词高亮。
  - 支持按前后 N 秒查询上下文。
  - 支持复制当前日志证据包。
- 回放体验增强：
  - 支持上一问题、下一问题跳转。
  - 支持区间循环播放，设置循环起点和终点。
  - 支持遇到异常点自动暂停。
  - 支持复制当前回放快照，作为轻量截图/证据包。
- 大日志性能基础：
  - 新增异步 session job API，前端可显示解析阶段和进度。
  - 新增日志索引缓存 bucket，缓存版本升级为 v2。
  - 时间线和错误码发生点支持服务端分页。
  - 新增 `/api/replay/event-markers`，进度条 marker 可走服务端聚合。
  - 缓存摘要增加版本、保留策略、清理建议和索引分类。
- 协作和诊断包：
  - 新增人工结论 API 和弹窗，支持现场、车辆、测试人员、测试轮次、处理状态、已确认根因和备注。
  - 报告和诊断包 manifest 包含人工结论。
  - 诊断包导出支持选项：时间范围、是否包含地图、报告、地图别名、根因反馈。
  - 新增诊断包 manifest 对比 API 和前端入口，可对比版本、车辆、地图、错误数、任务数、帧数。
  - 新增 `npm run replay:collect`，可把日志和地图收集成可导入的诊断包 zip。
- 产品化文档：
  - 新增 `docs/replay-productization-plan.md`，记录桌面应用、车端轻量部署和一键采集方向。

### 本轮暂未完全闭环的能力

- 诊断包按时间范围导出目前接口已接收 `startMs/endMs`，但日志文件仍按原文件复制，尚未裁剪到时间窗口。
- “截图”当前以复制当前回放状态快照为第一版实现，还不是画布图片导出。
- marker 已支持服务端聚合，但尚未实现点击聚合点展开事件列表和时间轴缩放。
- 诊断包对比当前是 manifest 基础对比，尚未对比错误码首次发生时间、任务失败点、定位分/电量/速度曲线。
- Electron/Tauri 桌面应用未引入依赖，本轮只完成产品化方案文档和采集脚本。

### 下一步建议

- 用真实现场样本补充 `replay-server/samples/manifest.example.json`，至少覆盖正常任务、地图缺失、错误码故障、定位分异常、任务失败、诊断包导入六类样本。
- 补充诊断包时间范围裁剪，避免大日志导出包仍然过大。
- 将复制回放快照升级为 Canvas 图片导出，并把图片路径写入报告附件。
- 强化诊断包对比，优先补错误码首次/末次时间、任务失败点和根因差异。
- 将服务端分页继续下沉到原始日志索引，减少超大日志过滤时的全量扫描。
- 根据现场反馈决定是否启动 Electron/Tauri 打包工作。

### 本轮验证记录

- `npm run typecheck`：通过。
- `npm run build`：通过。Vite 仍提示主 chunk 偏大，这是既有前端体积提醒，不影响运行。
- `npm run replay:build`：通过。
- `replay-server/scripts/verify-sample.ts`：通过，样本解析到 601 个回放帧、41 个错误发生点、3 个根因候选。
- `npm run replay:verify:samples`：通过。
- 验证期间生成的未跟踪 `.cache/imports`、`.cache/indexes`、`.cache/packages` 和临时 session 缓存已清理；Git 已跟踪的既有缓存文件未删除。

## 诊断知识库与自进化能力规划

本章节记录日志诊断工具下一阶段的核心方向：让工具不仅能解析固定规则，还能沉淀研发人员每一次真实排查经验。目标是让工具越用越懂现场问题，逐步从“日志回放与规则诊断工具”升级为“研发经验驱动的诊断知识库”。

### 总体目标

建立一条研发分析闭环：

1. 现场人员提供日志、地图或诊断包。
2. 工具自动完成日志解析、地图回放、错误码识别、时间线聚合和已有根因候选分析。
3. 研发人员查看自动诊断结果，同时在日志中发现工具暂未识别的问题。
4. 研发人员标记关键证据日志，填写问题描述、确认根因、处理办法和适用范围。
5. 工具把本次人工分析沉淀成一条可复用的诊断知识。
6. 下次遇到相似日志时，工具自动命中该知识，直接给出问题结论、证据链和处理建议。

这个方向需要分阶段实现。第一版优先做可解释、可审核、可导入导出的规则型知识库。第二版再引入机器学习、向量库和 DeepSeek 官方 API，实现相似案例检索和自然语言问答。

### 第一版：规则型诊断知识库

第一版不引入机器学习，重点做“研发可手工沉淀、工具可自动复用”的规则知识库。原因是日志诊断需要清晰证据链，第一版必须保证可解释、可调试、可回滚。

#### 第一版功能边界

- 知识条目管理：
  - 问题标题。
  - 问题描述。
  - 确认根因。
  - 处理办法。
  - 严重度：`info`、`warning`、`error`。
  - 标签：例如激光、定位、地图、任务、货叉、网络、配置。
  - 适用范围：项目、车型、地图、版本、分支，可选。
  - 启用/停用状态。
  - 创建时间、更新时间、创建人，可选。
- 证据规则：
  - 必须包含关键词。
  - 任意命中关键词。
  - 排除关键词。
  - 模块名匹配。
  - 日志等级匹配，例如 `[E]`、`[W]`。
  - 错误码匹配，例如 `ERRORxxxx`。
  - 时间窗口，例如 5 秒内同时出现 A、B 两类日志。
  - 次数阈值，例如 10 秒内某关键词出现超过 3 次。
  - 置信度加权，例如命中错误码加 0.3，命中关键模块加 0.2。
- 诊断命中输出：
  - 命中的知识名称。
  - 命中置信度。
  - 命中的规则项。
  - 关联证据日志。
  - 历史处理办法。
  - 适用范围提醒。
  - 是否来自知识库。
- 研发标注入口：
  - 从原始日志选择若干行作为证据。
  - 从时间线事件加入证据。
  - 从错误码发生点加入证据。
  - 从当前根因候选一键转为知识条目。
  - 自动从证据日志中提取候选关键词、模块名、错误码、日志等级和时间跨度。
- 知识库管理：
  - 列表查看。
  - 新增、编辑、删除。
  - 启用、停用。
  - 按标签、严重度、模块、错误码搜索。
  - 查看历史命中次数。
  - 查看最近命中样本。
- 导入导出：
  - JSON 导出完整知识库。
  - JSON 导入知识库。
  - 支持冲突提示，例如同名规则、同 ID 规则。
  - 支持覆盖或跳过。

#### 第一版建议数据结构

建议新增 `KnowledgeRule`：

```ts
interface KnowledgeRule {
  id: string
  title: string
  description: string
  rootCause: string
  solution: string
  severity: 'info' | 'warning' | 'error'
  tags: string[]
  enabled: boolean
  scope?: {
    project?: string
    robotType?: string
    mapName?: string
    version?: string
    branch?: string
  }
  pattern: KnowledgeEvidencePattern
  examples: KnowledgeExample[]
  hitCount: number
  createdAt: string
  updatedAt: string
}
```

建议新增 `KnowledgeEvidencePattern`：

```ts
interface KnowledgeEvidencePattern {
  requiredKeywords: string[]
  anyKeywords: string[]
  excludedKeywords: string[]
  modules: string[]
  levels: string[]
  errorCodes: string[]
  windowSeconds?: number
  minOccurrences?: number
  confidenceBase?: number
  confidenceWeights?: Array<{
    type: 'keyword' | 'module' | 'level' | 'errorCode'
    value: string
    weight: number
  }>
}
```

建议新增 `KnowledgeMatch`：

```ts
interface KnowledgeMatch {
  ruleId: string
  title: string
  confidence: number
  severity: 'info' | 'warning' | 'error'
  matchedPatterns: string[]
  evidenceLines: ParsedLogLine[]
  suggestion: string
}
```

#### 第一版建议文件规划

- 新增 `replay-server/config/knowledge-base.json`
  - 存储本地知识库规则。
  - 第一版使用 JSON 文件即可，降低部署复杂度。
- 新增 `replay-server/src/core/knowledgeBase.ts`
  - 读取知识库。
  - 保存知识库。
  - 导入导出。
  - 根据日志行匹配规则。
  - 输出 `KnowledgeMatch`。
- 修改 `replay-server/src/types.ts`
  - 增加 `KnowledgeRule`、`KnowledgeEvidencePattern`、`KnowledgeExample`、`KnowledgeMatch` 类型。
  - 给 `RootCauseCandidate` 增加可选字段 `source: 'built_in' | 'knowledge_base' | 'llm'`。
- 修改 `replay-server/src/core/rootCause.ts`
  - 在内置根因规则之后执行知识库匹配。
  - 将知识库命中转换为 `RootCauseCandidate`。
  - 保持根因排序时考虑知识库置信度。
- 修改 `replay-server/src/core/report.ts`
  - Markdown/JSON 报告输出知识库命中来源。
  - 报告中增加“知识库命中”小节。
- 修改 `replay-server/src/core/diagnosticPackage.ts`
  - 诊断包可选包含命中的知识规则快照。
  - 不默认把完整本地知识库全部打包，避免泄露无关研发经验。
- 修改 `replay-server/src/index.ts`
  - 新增知识库 CRUD API。
  - 新增知识库导入导出 API。
  - 新增从证据日志生成候选规则 API。
- 修改 `src/api/replay.ts`
  - 增加知识库接口封装。
- 修改 `src/stores/replay.ts`
  - 增加知识库列表、当前编辑规则、证据选择状态。
- 修改 `src/views/Replay.vue`
  - 增加证据标记入口。
  - 增加“沉淀知识”弹窗。
  - 增加“知识库管理”弹窗或独立 Tab。

#### 第一版前端交互规划

- 原始日志 Tab：
  - 每行日志增加选择框。
  - 支持多选日志作为证据。
  - 增加“用选中日志沉淀知识”按钮。
  - 增加“加入当前知识证据”按钮。
- 时间线 Tab：
  - 每条事件增加“沉淀知识”按钮。
  - 自动带入事件标题、详情、前后日志上下文。
- 错误码中心：
  - 每个错误码发生点增加“沉淀知识”按钮。
  - 自动带入错误码、模块、等级和上下文。
- 诊断结论：
  - 内置根因候选增加“转为知识”按钮。
  - 自动带入根因标题、建议、证据事件、证据日志。
- 沉淀知识弹窗：
  - 左侧展示证据日志。
  - 右侧填写标题、描述、根因、处理办法、严重度、标签。
  - 自动生成候选关键词、模块、错误码。
  - 研发可勾选哪些模式作为规则。
  - 保存前显示预计规则命中条件，避免规则过宽。
- 知识库管理：
  - 表格展示规则标题、严重度、标签、启用状态、命中次数、更新时间。
  - 支持编辑、复制、停用、删除。
  - 支持导入导出。
  - 支持用当前日志立即试跑某条规则。

#### 第一版后端规则匹配规划

- 单条规则匹配流程：
  - 先过滤排除关键词，命中排除则不匹配。
  - 再检查必须关键词，全部存在才继续。
  - 检查任意关键词，至少命中一个才继续。
  - 检查模块名、日志等级、错误码。
  - 如果配置时间窗口，则在窗口内聚合匹配证据。
  - 如果配置次数阈值，则检查命中次数。
  - 根据权重计算置信度。
- 匹配结果转根因：
  - `title` 使用知识标题。
  - `suggestion` 使用处理办法。
  - `evidenceLines` 使用命中的日志行。
  - `triggeredRules` 输出命中的模式项。
  - `positiveEvidence` 输出证据摘要。
  - `confidenceFactors` 输出置信度来源。
  - `source` 标记为 `knowledge_base`。
- 性能边界：
  - 第一版先基于 `rawLines` 扫描。
  - 如果知识库规则增多，再引入倒排索引或预编译正则。
  - 默认限制单条规则最多返回前 50 条证据，避免报告过大。

#### 第一版导入导出和多人同步

- 本地文件：
  - 默认存储在 `replay-server/config/knowledge-base.json`。
  - 适合单机研发使用。
- 导出：
  - 导出完整 JSON。
  - 包含规则版本、导出时间、规则数量。
- 导入：
  - 同 ID 规则提示冲突。
  - 支持跳过、覆盖、另存为副本。
- 多人同步：
  - 第一阶段通过 Git 或共享文件同步知识库 JSON。
  - 后续如果团队规模扩大，再考虑服务端知识库或数据库。
- 诊断包：
  - 诊断包内保存“本次命中的知识规则快照”，方便研发复现当时诊断依据。
  - 不自动包含全量知识库，避免将未公开规则或客户信息带出。

### 第二版：机器学习、向量库和 DeepSeek 问答

第二版建立在第一版规则知识库稳定之后，目标是让研发可以用自然语言询问当前日志，并通过相似案例检索快速找到历史经验。

#### 第二版核心能力

- 向量知识库：
  - 将知识条目、历史人工结论、处理办法、证据日志切分成文档块。
  - 为每个文档块生成 embedding。
  - 存入本地向量库。
  - 根据当前日志片段、错误码、根因候选检索相似历史案例。
- 相似案例推荐：
  - 在诊断结论旁展示“相似历史问题”。
  - 展示相似度、问题标题、处理办法、关键证据。
  - 支持一键打开历史案例详情。
- 自然语言问答：
  - 前端增加“问诊助手”。
  - 研发可以直接提问：
    - “这次问题最可能是什么？”
    - “这些激光超时日志以前遇到过吗？”
    - “这个 ERRORxxxx 应该怎么处理？”
    - “帮我总结这份日志的问题链路。”
    - “为什么任务没有成功完成？”
  - 后端根据当前诊断数据、原始日志片段、知识库命中、相似案例构造上下文。
  - 调用 DeepSeek 官方 API 返回结构化回答。
- DeepSeek 官方 API 接入：
  - 后端新增 LLM Provider 层。
  - 通过环境变量配置 `DEEPSEEK_API_KEY`。
  - 模型名、超时、最大 token、温度等参数可配置。
  - API Key 只在后端使用，不下发给前端。
  - 支持请求超时、重试和错误提示。
- RAG 流程：
  - 汇总当前诊断概览。
  - 提取用户问题。
  - 检索相关知识条目和历史案例。
  - 检索当前日志中的相关片段。
  - 拼接上下文。
  - 调用 DeepSeek。
  - 返回结论、证据、可能根因、处理建议、不确定点。

#### 第二版建议文件规划

- 新增 `replay-server/src/core/knowledgeEmbedding.ts`
  - 将知识条目和案例转换为可向量化文本块。
- 新增 `replay-server/src/core/vectorStore.ts`
  - 第一版可用本地 JSON/SQLite 存储向量。
  - 后续可替换为专业向量数据库。
- 新增 `replay-server/src/core/llmProvider.ts`
  - 定义统一 LLM 调用接口。
- 新增 `replay-server/src/core/deepseekClient.ts`
  - 封装 DeepSeek 官方 API。
- 新增 `replay-server/src/core/ragAssistant.ts`
  - 负责检索、上下文构造、问答编排。
- 新增 `replay-server/config/llm.example.json`
  - 记录 LLM 配置模板，不存真实密钥。
- 新增 `src/components/replay/ReplayAssistant.vue`
  - 前端问诊助手组件。

#### 第二版安全边界

- 默认仍以本地规则诊断为主，LLM 只作为辅助分析。
- LLM 输出必须标记为“AI 辅助建议”，不能直接覆盖根因结论。
- LLM 不允许自动写入知识库。
- 只有研发确认后，AI 建议才能转为知识条目。
- DeepSeek API Key 不进入前端、不进入诊断包、不写入 Git。
- 支持脱敏：
  - 本地路径脱敏。
  - IP 地址脱敏。
  - 客户名称或现场名称脱敏。
  - 车号按配置决定是否脱敏。
- 支持上下文长度限制：
  - 不上传全量日志。
  - 只上传当前问题相关片段、知识摘要和统计信息。
- 支持离线模式：
  - 未配置 API Key 时，规则知识库照常工作。
  - 页面隐藏或禁用问诊助手的在线能力。

### 推荐实施顺序

1. 第一阶段：规则知识库基础能力。
   - 完成知识库 JSON 存储。
   - 完成知识条目 CRUD。
   - 完成规则匹配。
   - 完成命中结果合并到根因候选。
2. 第二阶段：研发标注闭环。
   - 原始日志支持选择证据。
   - 时间线、错误码、根因候选支持沉淀知识。
   - 自动提取候选关键词、模块、错误码。
   - 支持知识库导入导出。
3. 第三阶段：知识库质量建设。
   - 增加规则试跑。
   - 增加命中次数统计。
   - 增加规则过宽/过窄提醒。
   - 建立真实样本回归测试，避免新增知识误报。
4. 第四阶段：相似案例检索。
   - 将知识条目和人工结论切块。
   - 引入本地向量库。
   - 在诊断结果中推荐相似历史问题。
5. 第五阶段：DeepSeek 问诊助手。
   - 接入 DeepSeek 官方 API。
   - 实现 RAG 问答。
   - 支持研发确认后将 AI 建议转为知识条目。
6. 第六阶段：团队协作和产品化。
   - 知识库版本管理。
   - 多人同步。
   - 规则审核。
   - 桌面端或车端采集流程中集成知识库版本信息。

### 第一版验收标准

- 研发可以从原始日志选择证据并保存为知识条目。
- 新建知识条目后，重新解析同一份日志能自动命中。
- 命中结果会出现在诊断结论中，并标记来源为知识库。
- 命中结果包含证据日志、置信度和处理办法。
- Markdown/JSON 报告能输出知识库命中。
- 知识库可以导入导出。
- 禁用某条知识后，该规则不再参与诊断。
- 样本验证可以覆盖至少 3 条知识库规则，避免误报和漏报。

### 第二版验收标准

- 配置 DeepSeek API Key 后，可以在页面自然语言提问。
- 问答结果能引用当前日志证据和知识库条目。
- 相似案例推荐能返回历史问题标题、处理办法和相似证据。
- 未配置 API Key 时，工具仍能正常使用规则知识库。
- AI 建议不会自动写入知识库，必须人工确认。
- 请求上下文不会上传全量日志，只上传相关片段和摘要。

## 规则型诊断知识库第一版实现记录

第一版规则型诊断知识库已进入实现，目标是先完成“研发标注证据 -> 保存知识规则 -> 下次自动命中 -> 输出到诊断结论和报告”的最小闭环。

### 已实现能力

- 新增本地知识库文件 `replay-server/config/knowledge-base.json`。
- 新增知识库类型：`KnowledgeRule`、`KnowledgeEvidencePattern`、`KnowledgeExample`、`KnowledgeMatch`。
- `RootCauseCandidate` 增加来源字段，可标记 `built_in`、`knowledge_base`、`llm`。
- 新增 `replay-server/src/core/knowledgeBase.ts`：
  - 知识库读写。
  - 规则增删改查。
  - 启用/停用。
  - 导入/导出。
  - 证据日志自动提取候选关键词、模块、错误码、等级。
  - 基于关键词、模块、等级、错误码、时间窗口、次数阈值的规则匹配。
  - 命中结果转换为根因候选。
- session 解析完成后会执行知识库匹配，并将命中结果写入 `knowledgeMatches`。
- 知识库命中会合并到诊断结论，并展示为“知识库命中”。
- Markdown/JSON 报告输出知识库命中。
- 诊断包导出 `config/knowledge-matches.json`，保存本次命中的知识规则快照。
- 新增知识库 API：
  - `GET /api/replay/knowledge`
  - `POST /api/replay/knowledge`
  - `PUT /api/replay/knowledge/:id`
  - `DELETE /api/replay/knowledge/:id`
  - `POST /api/replay/knowledge/:id/toggle`
  - `GET /api/replay/knowledge/export`
  - `POST /api/replay/knowledge/import`
  - `POST /api/replay/knowledge/suggest-pattern`
  - `POST /api/replay/knowledge/test`
- 前端 `/replay` 页面新增：
  - 顶部“知识库”入口。
  - 原始日志多选作为证据。
  - 时间线事件沉淀知识。
  - 错误码发生点沉淀知识。
  - 根因候选转为知识。
  - “沉淀诊断知识”弹窗。
  - “诊断知识库”管理弹窗。
  - 知识规则筛选、编辑、复制、删除、启用/停用、导入、导出、试跑。

### 第一版当前边界

- 第一版使用本地 JSON 文件存储，不引入数据库。
- 规则匹配以字符串包含为主，暂不做复杂正则编辑器。
- 时间窗口匹配已支持，但“多个事件必须按顺序出现”的复杂时序规则暂未实现。
- 规则过宽/过窄提醒暂未做成独立评分，仅通过试跑结果辅助研发判断。
- 诊断包只保存本次命中的知识快照，不导出全量本地知识库。
- DeepSeek、向量库、自然语言问答仍属于第二版能力。

### 第一版自动验收记录

本轮补充了专项验收脚本 `replay-server/scripts/verify-knowledge.ts`，并新增 npm 命令 `npm run replay:verify:knowledge`。脚本会在执行前备份 `replay-server/config/knowledge-base.json`，执行过程中临时写入验收规则，结束后恢复原知识库，避免污染研发本地知识。

已覆盖验收项：

- 模拟研发从原始日志选择证据，并保存为知识条目。
- 新建知识条目后，重新解析 `/home/xbl/Desktop/log-20260710-173240.log` 能自动命中。
- 命中结果进入 `overview.rootCauses`，并标记 `source: knowledge_base`。
- 命中结果包含证据日志、置信度、处理办法。
- Markdown 报告包含 `## 知识库命中`，JSON 报告包含 `knowledgeMatches`。
- 知识库导出后可重新导入，导入后的规则仍可命中。
- 禁用某条知识后，重新解析同一份日志不再命中该规则，其他规则不受影响。
- 样本验证覆盖 3 条规则：
  - `verify-knowledge-map-io-area`：地图缺少 IO 区域配置。
  - `verify-knowledge-battery-missing`：电池数据采集失败。
  - `verify-knowledge-empty-task-error-code`：任务错误码为空。

本轮验证命令：

- `npx -y -p node@20 -c 'npm run typecheck'`：通过。
- `npx -y -p node@20 -c 'npm run build'`：通过，仅保留 Vite chunk 偏大警告。
- `npx -y -p node@20 -c 'npm run replay:build'`：通过。
- `npx -y -p node@20 -c './node_modules/.bin/tsx replay-server/scripts/verify-sample.ts'`：通过。
- `npx -y -p node@20 -c 'npm run replay:verify:samples'`：通过。
- `npx -y -p node@20 -c 'npm run replay:verify:knowledge'`：通过，输出 `accepted: true`、`rules: 3`、`matches: 3`、`rootCauses: 3`、`imported: 3`。

仍建议补充的人工验收：

- 在浏览器中手动打开 `/replay`，从“原始日志”表格勾选证据行并保存知识条目。
- 在页面上确认知识库管理弹窗中的新增、编辑、复制、删除、导入、导出、启停、试跑交互体验。

### 后续优化建议

- 增加规则命中预览，显示哪些条件命中、哪些条件未命中。
- 增加规则误报反馈，支持从知识库命中直接标记“误报”。
- 增加知识规则样本绑定，每条规则保存正样本和负样本。
- 增加规则质量评分，提示规则过宽或证据过少。
- 增加知识库版本号和团队同步策略。

## 第二版第一轮实现记录：问诊助手、向量检索与 DeepSeek 接入

第二版第一轮目标是先把“相似经验检索 + 自然语言问答”的最小闭环接入 `/replay` 页面。实现边界为本地优先、DeepSeek 可选：未配置 API Key 时仍可使用规则知识库、相似知识检索和离线问答提示。

### 已实现能力

- 新增本地向量能力：
  - `replay-server/src/core/knowledgeEmbedding.ts`
  - `replay-server/src/core/vectorStore.ts`
  - 第一轮使用本地 hash embedding，不依赖外部 embedding 服务。
  - 向量库文件位于 `replay-server/.cache/vector-store.json`。
- 新增 DeepSeek 配置与调用：
  - `replay-server/config/llm.example.json`
  - `replay-server/src/core/llmConfig.ts`
  - `replay-server/src/core/llmProvider.ts`
  - `replay-server/src/core/deepseekClient.ts`
  - API Key 只从后端环境变量 `DEEPSEEK_API_KEY` 读取，不下发前端。
- 新增上下文脱敏：
  - `replay-server/src/core/redaction.ts`
  - 支持本地路径、IP、长 ID、车号可选脱敏。
- 新增 RAG 编排：
  - `replay-server/src/core/ragAssistant.ts`
  - 汇总诊断概览、根因候选、知识库命中、相似知识块和相关日志片段。
  - 限制日志片段数量，默认不上传全量日志。
  - 未配置 DeepSeek 时返回离线建议。
  - DeepSeek 调用失败时自动降级为离线建议。
- 新增后端 API：
  - `GET /api/replay/assistant/status`
  - `POST /api/replay/assistant/reindex`
  - `GET /api/replay/assistant/similar`
  - `POST /api/replay/assistant/context-preview`
  - `POST /api/replay/assistant/ask`
- 新增前端能力：
  - `src/components/replay/ReplayAssistant.vue`
  - `/replay` 页面新增“问诊助手”Tab。
  - 支持查看 DeepSeek/离线状态。
  - 支持重建向量索引。
  - 支持相似历史问题列表。
  - 支持快捷问题、上下文预览和提问。
  - 支持把 AI 辅助建议转为知识草稿，但仍需研发人工确认保存。
- 报告与诊断包：
  - JSON 报告新增 `assistant` 和 `similarCases`。
  - 诊断包新增 `config/assistant-snapshot.json`，只保存本次 AI/离线回答快照，不包含 API Key 和全量向量库。
- 新增验证脚本：
  - `replay-server/scripts/verify-assistant.ts`
  - npm 命令：`npm run replay:verify:assistant`

### 第一轮边界

- 当前 embedding 是本地轻量 hash embedding，适合先做可离线检索，不等价于深度学习语义向量。
- 当前向量库只索引本地知识规则和人工结论，历史诊断包批量入库后续再做。
- DeepSeek 只做辅助问答，不自动覆盖根因结论，不自动写入知识库。
- AI 建议转知识只打开草稿弹窗，必须人工确认后保存。
- 上下文预览用于研发确认将要发送的摘要和日志片段。

### 后续优化点

- 引入真实 embedding API 或本地 embedding 模型，替换 hash embedding。
- 支持历史诊断包批量入库，形成跨现场案例库。
- 增加相似案例详情页，展示历史问题完整证据链。
- 增加 LLM 回答引用定位，点击证据跳转时间线或地图回放。
- 增加团队级知识库版本管理和同步策略。

### 第二版自动验收记录

本轮强化 `replay-server/scripts/verify-assistant.ts`，将 `### 第二版验收标准` 转成可重复执行的专项验收。由于当前机器未配置真实 `DEEPSEEK_API_KEY`，在线问答链路使用本地 OpenAI-compatible mock DeepSeek 服务验证：后端仍按 DeepSeek API 格式发起请求，并通过 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL` 进入在线分支。

逐项验收结果：

- 配置 DeepSeek API Key 后，可以在页面自然语言提问：
  - 已通过 mock DeepSeek 验证在线问答链路，返回 `onlineProvider: deepseek`。
  - 后端状态接口在配置 Key 后返回在线模式。
- 问答结果能引用当前日志证据和知识库条目：
  - 验收脚本临时创建“电池数据采集失败历史处理”知识规则。
  - 在线回答同时包含 `knowledge_base` 证据和 `log` 证据。
- 相似案例推荐能返回历史问题标题、处理办法和相似证据：
  - 相似案例返回标题 `验收助手：电池数据采集失败历史处理`。
  - 返回处理办法 `检查电池设备配置、驱动启动状态和通信连接。`。
  - 返回知识规则证据日志或相似高亮。
- 未配置 API Key 时，工具仍能正常使用规则知识库：
  - 删除 `DEEPSEEK_API_KEY` 后，知识库规则仍能命中当前日志。
  - 离线问答返回 `offlineProvider: offline`。
- AI 建议不会自动写入知识库，必须人工确认：
  - 调用问诊助手前后知识库规则数量保持 `[1, 1]`。
  - 前端“转为知识”只打开草稿弹窗，不自动保存。
- 请求上下文不会上传全量日志，只上传相关片段和摘要：
  - 样本总日志行数 `165902`。
  - 问答上下文日志片段数 `20`。
  - DeepSeek 请求上下文改为 compact 摘要，避免完整嵌套日志对象。
  - 路径、IP、长 ID 脱敏通过，示例输出 `[PATH] [IP] [ID]`。

本轮发现并修复的问题：

- DeepSeek 请求体原先直接序列化完整 context，`overview.topIssues` 嵌套过大，导致 `knowledgeMatches` 被 60000 字符限制截断；已改为 compact 摘要。
- overview 中的 `logDir` 等字符串原先未脱敏；已让 `redactAssistantContext` 递归脱敏 overview。
- 相似案例列表原先没有显式展示处理办法和相似证据；已在向量 source 和前端表格中补充。

本轮验证命令：

- `npx -y -p node@20 -c 'npm run typecheck'`：通过。
- `npx -y -p node@20 -c 'npm run build'`：通过，仅保留 Vite chunk 偏大警告。
- `npx -y -p node@20 -c 'npm run replay:build'`：通过。
- `npx -y -p node@20 -c 'npm run replay:verify:samples'`：通过。
- `npx -y -p node@20 -c 'npm run replay:verify:knowledge'`：通过。
- `npx -y -p node@20 -c 'npm run replay:verify:assistant'`：通过。

## 问诊助手前端可配置模型实现记录

本轮补充“配置模型”能力，解决只能通过后端环境变量配置 DeepSeek 的问题。现在研发可以在 `/replay` 的“问诊助手”Tab 中配置 Provider、API Key、Base URL、Model、Timeout、Max Tokens 和 Temperature。

### 已实现能力

- 新增本地配置文件 `replay-server/config/llm.local.json`。
- `.gitignore` 已忽略 `replay-server/config/llm.local.json`，避免 API Key 进入 Git。
- 新增 `replay-server/src/core/llmConfigStore.ts`：
  - 读取本地配置。
  - 保存本地配置。
  - 清空本地配置。
  - API Key 脱敏。
- `llmConfig` 优先级：
  - 本地配置文件。
  - 环境变量。
  - 默认值。
- 新增通用 OpenAI-compatible client：
  - `replay-server/src/core/openAiCompatibleClient.ts`
  - 支持 DeepSeek。
  - 支持其他兼容 `/chat/completions` 的模型服务。
- 新增后端接口：
  - `GET /api/replay/assistant/config`
  - `POST /api/replay/assistant/config`
  - `DELETE /api/replay/assistant/config`
  - `POST /api/replay/assistant/config/test`
- 前端“问诊助手”新增“配置模型”弹窗：
  - Provider：DeepSeek / OpenAI Compatible。
  - API Key：密码框，留空不修改旧 Key。
  - Base URL。
  - Model。
  - Timeout。
  - Max Tokens。
  - Temperature。
  - 测试连接。
  - 保存。
  - 清空本地配置。

### 安全边界

- 前端只显示 masked key，例如 `****3456`。
- 后端 config API 不返回完整 API Key。
- 诊断包不导出 `llm.local.json`。
- 诊断包不包含 API Key。
- 清空本地配置后会回退到环境变量或离线模式。

### 验收覆盖

`npm run replay:verify:assistant` 已增加：

- 保存本地 LLM 配置。
- 公开配置只返回 masked key。
- status 使用 `local_file` 来源。
- OpenAI Compatible mock provider 可在线问答。
- 验证诊断包不包含 API Key 和 `llm.local.json`。
- 验证脚本结束后恢复原 `llm.local.json`。
